import Vue from 'vue'
import Router from 'vue-router'
import Frame0 from '@/components/Frame-0'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
      name: 'Frame0',
      component: Frame0
    }
  ]
})
